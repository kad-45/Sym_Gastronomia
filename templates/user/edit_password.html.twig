{% extends 'base.html.twig' %}

{% block title %}symgastronomia - Modification mot de passe{% endblock %}

{% block body %}

    <div class="container w-50">
        <h2 class="mt-3">Modification mot de passe d'utilisateur</h2>
        {% for message in app.flashes('warning') %}
            <div class="alert alert-warning mt-4">
                {{ message }}
            </div>
        {% endfor %}
        {{ form(form) }}
    </div>
{% endblock %}